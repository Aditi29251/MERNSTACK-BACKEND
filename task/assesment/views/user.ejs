<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        // Q1.
        let arr=[10,20,30,40,50]

        const newArr=[]
        for(let i=0;i<arr.length;i++)
    {
        newArr[i]=arr[i]*2
    }

    console.log(newArr)

    // Q2.
    arr1=[10,20,30,40,50]

    arr1.sort((a,b)=>b-a)

    let secondArr=arr1[1]

    console.log(secondArr)

    // Q3

    arr2=[10,20,30,40,50]
    let reversed=[];
    for(let i=arr2.length-1; i>=0; i--){
            reversed.push(arr2[i])
    }
    console.log(reversed)

    // Q3. 

    let users=[
        {name:"Amit",age:22},
        {name:"Rahul",age:18},
        {name:"Sneha",age:25}
    ];

    console.log(users)

    let result=users.filter(user=>user.age>20)
    console.log(result)

   
        // 4

        let array=[1,2,3,2,4,2,5];
        let num=2;

        let count= arr.filter(value=>value===num).length;

        console.log(array)
        console.log(count)

        // Q2
        let array1 = [1, 2, [3, 4], [5, [6, 7]]];

        let flatArray = array1.flat(Infinity);

        console.log(flatArray);


        // Q

        let arr4=[1,2,3,4]

        let lastElement=arr.pop();
        arr4.unshift(lastElement);

        console.log(arr4)

        // Logic Base Q1
        // 1.
            let string="Aditi";
            console.log(string.split('').reverse().join())

        // 2.
          function isPalindrome(str) {

  const result1 = str.toLowerCase().replace(/[^a-z0-9]/g, '');

  return result1 === result1.split('').reverse().join('');
}

console.log(isPalindrome("Racecar"));
console.log(isPalindrome("Hello, world!"));
console.log(isPalindrome("A man, a plan, a canal, Panama"));

// Q
function capital(str) {
  return str.split(' ').map(word => word[0].toUpperCase() + word.slice(1)).join(' ');
}
console.log(capital("hello world"));



    let str=" MERN Stck ";
    console.log(str.trim())
    </script>
</body>
</html>